{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "install",
            "type": "shell",
            "command": "rsync -av -zr -e 'ssh -p 22 -o StrictHostKeychecking=no' ${workspaceFolder}/ root@172.31.97.159:/usr/local/mgr5/src/plugin-examples/",
            "dependsOn": [
                "build"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}